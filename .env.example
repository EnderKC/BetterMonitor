# JWT 密钥必须在生产环境中通过运行时环境变量提供。
# JWT_SECRET=replace_with_a_random_secret

# 数据库路径（仅非 Docker 环境需要）
DB_PATH=./data/data.db

# Backend 监听端口（仅非 Docker 环境需要）
PORT=8085

# Dashboard / Agent 版本基线
DASHBOARD_VERSION=1.2.5
AGENT_VERSION=1.2.5
